- Change all #!/bin/ksh references to #!/bin/sh, at least for the build
  time requirements.  Runtime requirements may still insist on ksh.

- Patch installCDE so it installs to locations writable by a non-root
  user.

- Add a gdm session script to the package.

- Figure out what files are necessary for building docs and build docs.

- Get dtksh building.

- Add an init script for the calendar rpc service.

- Figure out what iptables rules are necessary for ToolTalk.
